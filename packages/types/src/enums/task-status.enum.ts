export enum TaskStatus {
    PLANNED   = 'planned',
    SCHEDULED = 'scheduled',
    ACTIVE    = 'active',
    BLOCKED   = 'blocked',
    DONE      = 'done',
    SLIPPED   = 'slipped',
    CANCELLED = 'cancelled',
  }